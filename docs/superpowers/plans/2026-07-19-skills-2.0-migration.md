# Plan de Implementación: Migración a Skills 2.0

> **Para workers agénticos:** SUB-SKILL REQUERIDO: usa superpowers:subagent-driven-development (recomendado) o superpowers:executing-plans para implementar este plan tarea por tarea. Los pasos usan sintaxis de checkbox (`- [ ]`) para el seguimiento.

**Objetivo:** Migrar los skills propios al formato Skills 2.0 (SKILL.md + skill.yaml + schema.json), fusionar los tres skills de arquitectura en uno, hacer fork propio de ui-ux-pro-max, y eliminar repo-map y dcp-lite.

**Arquitectura:** Convención de sidecars validada por un script Node sin dependencias. El validador se escribe PRIMERO (es el arnés de pruebas); las tareas de migración lo ponen en verde. Los componentes del instalador mantienen el patrón `ac_component_*` existente.

**Stack tecnológico:** Bash (instalador), Node ≥18 solo builtins (validador), Markdown/YAML/JSON (skills).

**Spec:** [2026-07-19-skills-2.0-migration-design.md](../specs/2026-07-19-skills-2.0-migration-design.md)

**Raíz del repo:** `c:\Users\JHONNY\Desktop\WORKSPACE\Herramientas\ABSOLUTE-CLAUDE` (las rutas de Git Bash de abajo son relativas a ella). Política de modelos: los spawns de subagente usan Sonnet 5; las revisiones permanecen en la sesión.

**Regla de commits:** Conventional Commits, sin footers "Co-authored-by" / de atribución de IA, subject ≤50 caracteres.

---

### Tarea 1: Validador (arnés de pruebas primero)

**Archivos:**
- Crear: `skills/validate-skills.mjs`

- [ ] **Paso 1: Escribir el validador**

Crear `skills/validate-skills.mjs` con exactamente este contenido:

```js
#!/usr/bin/env node
// Validates every skill directory under skills/ against the Skills 2.0 contract:
//   SKILL.md   — frontmatter with name (== dir name) and description
//   skill.yaml — name (== dir name), version, kind, triggers, schema
//   schema.json — valid JSON with definitions.inputs and definitions.outputs
//   every entry in skill.yaml `scripts` exists on disk
// Exit 0 when all pass; exit 1 with an error listing otherwise.
// Dependency-free: node: built-ins only. Parses the small YAML subset we use
// (top-level `key: value` and `key: [a, b]` / block lists with "- ").

import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const skillsDir = path.dirname(fileURLToPath(import.meta.url));
const errors = [];

function parseFrontmatter(md) {
    const m = md.match(/^---\r?\n([\s\S]*?)\r?\n---/);
    if (!m) return null;
    return parseMiniYaml(m[1]);
}

function parseMiniYaml(text) {
    const out = {};
    let listKey = null;
    for (const raw of text.split(/\r?\n/)) {
        if (!raw.trim() || raw.trim().startsWith("#")) continue;
        const listItem = raw.match(/^\s+-\s+(.*)$/);
        if (listItem && listKey) {
            out[listKey].push(stripQuotes(listItem[1]));
            continue;
        }
        const kv = raw.match(/^([A-Za-z_][\w-]*):\s*(.*)$/);
        if (!kv) continue;
        const [, key, valRaw] = kv;
        const val = valRaw.trim();
        if (val === "" || val === "|" || val === ">") {
            out[key] = [];
            listKey = key;
        } else if (val.startsWith("[")) {
            out[key] = val.replace(/^\[|\]$/g, "").split(",")
                .map(s => stripQuotes(s.trim())).filter(Boolean);
            listKey = null;
        } else {
            out[key] = stripQuotes(val);
            listKey = null;
        }
    }
    return out;
}

function stripQuotes(s) {
    return s.replace(/^["']|["']$/g, "");
}

const dirs = fs.readdirSync(skillsDir, { withFileTypes: true })
    .filter(d => d.isDirectory())
    .map(d => d.name);

for (const dir of dirs) {
    const base = path.join(skillsDir, dir);
    const fail = msg => errors.push(`${dir}: ${msg}`);

    // SKILL.md
    const skillMd = path.join(base, "SKILL.md");
    if (!fs.existsSync(skillMd)) { fail("missing SKILL.md"); continue; }
    const fm = parseFrontmatter(fs.readFileSync(skillMd, "utf8"));
    if (!fm) fail("SKILL.md has no parseable frontmatter");
    else {
        if (fm.name !== dir) fail(`SKILL.md frontmatter name "${fm.name}" != dir name`);
        if (!fm.description) fail("SKILL.md frontmatter missing description");
    }

    // skill.yaml
    const yamlPath = path.join(base, "skill.yaml");
    if (!fs.existsSync(yamlPath)) { fail("missing skill.yaml"); continue; }
    const cfg = parseMiniYaml(fs.readFileSync(yamlPath, "utf8"));
    if (cfg.name !== dir) fail(`skill.yaml name "${cfg.name}" != dir name`);
    for (const req of ["version", "kind", "schema"]) {
        if (!cfg[req]) fail(`skill.yaml missing ${req}`);
    }
    if (!Array.isArray(cfg.triggers) || cfg.triggers.length === 0) {
        fail("skill.yaml triggers must be a non-empty list");
    }
    if (cfg.kind && !["knowledge", "tool"].includes(cfg.kind)) {
        fail(`skill.yaml kind "${cfg.kind}" not knowledge|tool`);
    }
    for (const s of cfg.scripts || []) {
        if (!fs.existsSync(path.join(base, s))) fail(`declared script missing: ${s}`);
    }

    // schema.json
    const schemaPath = path.join(base, "schema.json");
    if (!fs.existsSync(schemaPath)) { fail("missing schema.json"); continue; }
    let schema;
    try { schema = JSON.parse(fs.readFileSync(schemaPath, "utf8")); }
    catch (e) { fail(`schema.json invalid JSON: ${e.message}`); continue; }
    if (!schema.definitions?.inputs) fail("schema.json missing definitions.inputs");
    if (!schema.definitions?.outputs) fail("schema.json missing definitions.outputs");
}

if (errors.length) {
    console.error(`validate-skills: ${errors.length} error(s)`);
    for (const e of errors) console.error("  - " + e);
    process.exit(1);
}
console.log(`validate-skills: OK (${dirs.length} skills)`);
```

- [ ] **Paso 2: Ejecutarlo — se esperan FALLOS (los sidecars aún no existen)**

Ejecutar: `node skills/validate-skills.mjs`
Esperado: sale con 1, errores como `solid: missing skill.yaml`, `repo-map: missing skill.yaml`, etc. Este es el estado rojo; las tareas 2–7 lo ponen en verde.

- [ ] **Paso 3: Commit**

```bash
git add skills/validate-skills.mjs
git commit -m "feat(skills): add Skills 2.0 validator"
```

---

### Tarea 2: Eliminar repo-map

**Archivos:**
- Eliminar: `skills/repo-map/` (directorio completo), `bin/components/repo-map.sh`
- Modificar: `install.sh:4-5` (comentario de cabecera), `install.sh:57`, `install.sh:165-168`, `install.sh:221`

- [ ] **Paso 1: Eliminar archivos**

```bash
git rm -r skills/repo-map
git rm bin/components/repo-map.sh
```

- [ ] **Paso 2: Editar install.sh**

Línea 57 — eliminar `repo-map` de la lista de componentes:

```bash
ALL_COMPONENTS=(rtk caveman dcp figma ui-ux dev-skills)
```

Líneas 165–168 — eliminar todo el bloque case `repo-map)`:

```bash
        repo-map)
            . "$AC_REPO_DIR/bin/components/repo-map.sh"
            ac_component_repo_map
            ;;
```

Línea 221 — eliminar la línea `/repomap` del heredoc final "Try the commands". Comentario de cabecera (líneas 4–5): quitar "repo-map skill," de la enumeración de componentes.

- [ ] **Paso 3: Verificar que nada lo referencia**

Ejecutar: `grep -rn "repo-map\|repo_map\|repomap" --include="*.sh" .`
Esperado: solo hay coincidencias en `uninstall.sh` (la limpieza legada se mantiene — requisito de la spec).

- [ ] **Paso 4: Commit**

```bash
git add -A
git commit -m "feat(skills)!: remove repo-map (replaced by Graphify)"
```

---

### Tarea 3: Eliminar dcp-lite

**Archivos:**
- Eliminar: `skills/dcp-lite/` (directorio completo), `hooks/dcp-lite-dedup.mjs` (y `hooks/` si queda vacío), `bin/components/dcp.sh`
- Modificar: `install.sh:4` (comentario de cabecera), `install.sh:57`, `install.sh:161-164`, `install.sh:221-222`

- [ ] **Paso 1: Eliminar archivos**

```bash
git rm -r skills/dcp-lite hooks
git rm bin/components/dcp.sh
```

- [ ] **Paso 2: Editar install.sh**

Línea 57 — eliminar `dcp`:

```bash
ALL_COMPONENTS=(rtk caveman figma ui-ux dev-skills)
```

Eliminar el bloque case `dcp)` (líneas 161–164):

```bash
        dcp)
            . "$AC_REPO_DIR/bin/components/dcp.sh"
            ac_component_dcp
            ;;
```

Heredoc final — eliminar las líneas `/dcp-compress` y `/dcp-context`. Comentario de cabecera línea 4: quitar "DCP (real for opencode + dcp-lite skill for Claude Code),".

- [ ] **Paso 3: Verificar**

Ejecutar: `grep -rn "dcp" --include="*.sh" .`
Esperado: solo hay coincidencias en `uninstall.sh` (sus bloques de limpieza de dcp-lite + DCP de opencode se mantienen para instalaciones existentes).

- [ ] **Paso 4: Commit**

```bash
git add -A
git commit -m "feat(skills)!: remove dcp-lite (RAG brain supersedes)"
```

---

### Tarea 4: Fusionar en architecture-principles

**Archivos:**
- Crear: `skills/architecture-principles/SKILL.md`, `skills/architecture-principles/skill.yaml`, `skills/architecture-principles/schema.json`
- Eliminar: `skills/solid/`, `skills/design-patterns/`, `skills/architecture-patterns/`

- [ ] **Paso 1: Crear el SKILL.md fusionado**

```bash
mkdir -p skills/architecture-principles
strip_fm() { awk 'BEGIN{n=0} /^---[[:space:]]*$/{n++; next} n>=2' "$1"; }
{
cat <<'EOF'
---
name: architecture-principles
description: "Unified engineering-architecture skill: SOLID principles (SRP, OCP, LSP, ISP, DIP), GoF design patterns (creational/structural/behavioral), and system-level architectures (layered, hexagonal, clean, onion, MVC/MVVM, microservices, monolith, event-driven, CQRS, serverless). Trigger on "SOLID", "design pattern", "factory", "strategy", "observer", "architecture", "structure the project", "hexagonal", "microservices", "refactor for cleaner design", or when designing/reviewing/refactoring components and systems."
---

# Architecture Principles

Merged from the former `solid`, `design-patterns`, and `architecture-patterns` skills. Three altitude levels of one discipline: principles (SOLID) → component patterns (GoF) → system architectures.

## Part 1 — SOLID principles
EOF
strip_fm skills/solid/SKILL.md
echo; echo "## Part 2 — Design patterns"
strip_fm skills/design-patterns/SKILL.md
echo; echo "## Part 3 — System architectures"
strip_fm skills/architecture-patterns/SKILL.md
echo; echo "Config: skill.yaml · Schema: schema.json"
} > skills/architecture-principles/SKILL.md
```

Luego abrir el archivo fusionado y: degradar el H1 original de cada parte a texto plano o eliminarlo (el nuevo H1 es "Architecture Principles"); actualizar los wiki-links `[[solid]]`, `[[design-patterns]]`, `[[architecture-patterns]]` en las secciones "See also" a referencias internas ("ver Parte 1/2/3 arriba") y dejar solo `[[conventional-commits]]` como enlace externo.

- [ ] **Paso 2: Crear skill.yaml**

`skills/architecture-principles/skill.yaml`:

```yaml
name: architecture-principles
version: 2.0.0
kind: knowledge
triggers:
  - "SOLID"
  - "design pattern"
  - "factory / strategy / observer / decorator"
  - "architecture"
  - "structure the project"
  - "hexagonal / clean architecture / microservices / monolith"
  - "refactor for cleaner design"
commands: []
scripts: []
dependencies:
  - conventional-commits
schema: ./schema.json
```

- [ ] **Paso 3: Crear schema.json**

`skills/architecture-principles/schema.json`:

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "architecture-principles",
  "definitions": {
    "inputs": {
      "type": "object",
      "properties": {
        "context": {
          "type": "string",
          "description": "Optional free-form description of the code, component, or system under discussion"
        },
        "level": {
          "type": "string",
          "enum": ["principles", "patterns", "system"],
          "description": "Which altitude to apply: SOLID (principles), GoF (patterns), or system architecture (system). Omit to let the skill pick."
        }
      },
      "additionalProperties": false
    },
    "outputs": {
      "type": "object",
      "properties": {
        "recommendation": {
          "type": "string",
          "description": "Named principle/pattern/architecture with rationale and trade-offs"
        },
        "anti_patterns": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Pitfalls to avoid for the chosen approach"
        }
      },
      "required": ["recommendation"]
    }
  }
}
```

- [ ] **Paso 4: Eliminar los tres directorios de skills antiguos**

```bash
git rm -r skills/solid skills/design-patterns skills/architecture-patterns
```

- [ ] **Paso 5: Ejecutar el validador**

Ejecutar: `node skills/validate-skills.mjs`
Esperado: sin errores que mencionen `architecture-principles`, `solid`, `design-patterns` ni `architecture-patterns`. (Los errores de `conventional-commits` y `ui-ux-pro-max` permanecen hasta las tareas 5–6.)

- [ ] **Paso 6: Commit**

```bash
git add -A
git commit -m "feat(skills)!: merge SOLID+patterns+arch into architecture-principles"
```

---

### Tarea 5: Sidecars de conventional-commits

**Archivos:**
- Crear: `skills/conventional-commits/skill.yaml`, `skills/conventional-commits/schema.json`
- Modificar: `skills/conventional-commits/SKILL.md` (añadir una línea)

- [ ] **Paso 1: Crear skill.yaml**

```yaml
name: conventional-commits
version: 2.0.0
kind: knowledge
triggers:
  - "commit"
  - "commit message"
  - "changelog"
  - "PR title"
  - "what should this commit be"
commands: []
scripts: []
dependencies:
  - architecture-principles
schema: ./schema.json
```

- [ ] **Paso 2: Crear schema.json**

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "conventional-commits",
  "definitions": {
    "inputs": {
      "type": "object",
      "properties": {
        "diff": {
          "type": "string",
          "description": "Staged diff or change summary (the skill runs git diff --staged --stat itself when omitted)"
        },
        "breaking": {
          "type": "boolean",
          "description": "Force the ! breaking-change marker and BREAKING CHANGE footer"
        }
      },
      "additionalProperties": false
    },
    "outputs": {
      "type": "object",
      "properties": {
        "message": {
          "type": "string",
          "description": "Full commit message: <type>(<scope>): <subject> (subject <=50 chars, imperative, no trailing period), optional body wrapped at 72, footers per Conventional Commits 1.0.0. Never includes Co-authored-by: Claude or AI attribution."
        }
      },
      "required": ["message"]
    }
  }
}
```

- [ ] **Paso 3: Añadir el puntero al sidecar en SKILL.md**

Añadir como línea final de `skills/conventional-commits/SKILL.md`:

```markdown
Config: skill.yaml · Schema: schema.json
```

También actualizar sus wiki-links "See also": cualquiera de `[[solid]]` / `[[design-patterns]]` / `[[architecture-patterns]]` pasa a ser `[[architecture-principles]]`.

- [ ] **Paso 4: Ejecutar el validador**

Ejecutar: `node skills/validate-skills.mjs`
Esperado: sin errores de `conventional-commits`.

- [ ] **Paso 5: Commit**

```bash
git add skills/conventional-commits
git commit -m "feat(skills): migrate conventional-commits to 2.0"
```

---

### Tarea 6: Fork propio de ui-ux-pro-max + sidecars

**Archivos:**
- Crear: `skills/ui-ux-pro-max/` (contenido forkeado + `skill.yaml` + `schema.json`)

- [ ] **Paso 1: Hacer fork del contenido upstream**

La fuente preferida es el clon ya instalado; si no existe, se recurre a un clon temporal nuevo:

```bash
SRC="$HOME/.claude/skills/ui-ux-pro-max"
if [ ! -d "$SRC" ]; then
  SRC="$(mktemp -d)/ui-ux-pro-max"
  git clone --depth 1 https://github.com/nextlevelbuilder/ui-ux-pro-max-skill "$SRC"
fi
cp -R "$SRC" skills/ui-ux-pro-max
rm -rf skills/ui-ux-pro-max/.git
```

- [ ] **Paso 2: Crear skill.yaml**

Inspeccionar el frontmatter del SKILL.md forkeado; si su `name` no es `ui-ux-pro-max`, cambiarlo a `ui-ux-pro-max` (el validador exige dir == name). Luego crear `skills/ui-ux-pro-max/skill.yaml`:

```yaml
name: ui-ux-pro-max
version: 2.0.0
kind: tool
triggers:
  - "UI / UX / design / mockup"
  - "landing page / dashboard / e-commerce / SaaS"
  - "button / modal / navbar / card / form / chart"
  - "glassmorphism / brutalism / dark mode / responsive"
  - "color palette / typography / accessibility / animation"
  - "transition / motion / framer-motion / gsap"
  - "brand guidelines / brand identity"
commands: []
scripts: []
dependencies: []
schema: ./schema.json
```

(Si el fork trae scripts — comprobar con `ls skills/ui-ux-pro-max/` — listar cada uno bajo `scripts:` para que el validador los revise.)

- [ ] **Paso 3: Crear schema.json**

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ui-ux-pro-max",
  "definitions": {
    "inputs": {
      "type": "object",
      "properties": {
        "task": {
          "type": "string",
          "enum": ["plan", "build", "review", "fix", "improve"],
          "description": "What to do with the UI"
        },
        "target": {
          "type": "string",
          "description": "Component, page, or app under work (e.g. 'pricing page', 'navbar')"
        },
        "stack": {
          "type": "string",
          "description": "Tech stack, e.g. react, next, vue, svelte, swiftui, react-native, flutter, tailwind, shadcn, html-css"
        },
        "style": {
          "type": "string",
          "description": "Visual style, e.g. glassmorphism, brutalism, minimalism, dark-mode"
        }
      },
      "additionalProperties": false
    },
    "outputs": {
      "type": "object",
      "properties": {
        "design_or_code": {
          "type": "string",
          "description": "Design guidance or implementation code for the requested stack, honoring style, accessibility, and brand rules"
        }
      },
      "required": ["design_or_code"]
    }
  }
}
```

- [ ] **Paso 4: Añadir el puntero al sidecar**

Añadir como línea final de `skills/ui-ux-pro-max/SKILL.md`:

```markdown
Config: skill.yaml · Schema: schema.json
```

- [ ] **Paso 5: Ejecutar el validador**

Ejecutar: `node skills/validate-skills.mjs`
Esperado: `validate-skills: OK (3 skills)` — el árbol ya está en verde.

- [ ] **Paso 6: Commit**

```bash
git add skills/ui-ux-pro-max
git commit -m "feat(skills): fork ui-ux-pro-max first-party as 2.0"
```

---

### Tarea 7: Consolidar skills de diseño en ui-ux-pro-max

**Archivos:**
- Modificar: `skills/ui-ux-pro-max/SKILL.md` (o su directorio references/ si el fork usa uno)

- [ ] **Paso 1: Localizar las fuentes upstream**

Buscar en la web cada uno de: `anthropics/skills frontend-design`, `anthropics/skills brand-guidelines`, `claude skill "taste"`, `claude skill "webarticast"`. Traer el SKILL.md de cada resultado. **Si no se encuentra una fuente, omitirla y anotar la falta en el cuerpo del commit** (la spec lo permite).

- [ ] **Paso 2: Destilar dentro del fork**

Para cada skill encontrado, añadir una sección a `skills/ui-ux-pro-max/SKILL.md` (antes de la línea del puntero al sidecar), de 30 a 80 líneas cada una, conservando las reglas accionables y descartando el contenido duplicado que el fork ya cubre:

```markdown
## Consolidated: frontend design principles
<distilled rules from anthropics frontend-design>

## Consolidated: brand guidelines
<distilled rules from anthropics brand-guidelines>

## Consolidated: taste
<distilled rules, if source found>

## Consolidated: webarticast
<distilled rules, if source found>

## Motion: transitions with Framer Motion + GSAP
- Transitions are the applied form of motion design: entrance/exit, layout, scroll-driven.
- Framer Motion for React component/layout transitions (`motion.div`, `AnimatePresence`, `layout` prop).
- GSAP for timeline-based, scroll-triggered (ScrollTrigger), and non-React contexts.
- Respect `prefers-reduced-motion`; keep durations 150-400ms for UI, springs for natural feel.
- npm deps `framer-motion` + `gsap` are installed by the ui-ux installer component.
```

(Los placeholders `<distilled rules ...>` de arriba los rellena quien ejecute el plan con las fuentes obtenidas en el Paso 1 — texto real de las reglas, no resúmenes de resúmenes.)

- [ ] **Paso 3: Validar + commit**

Ejecutar: `node skills/validate-skills.mjs` → Esperado: `OK (3 skills)`.

```bash
git add skills/ui-ux-pro-max
git commit -m "feat(skills): consolidate design skills into ui-ux-pro-max"
```

El cuerpo del commit lista cuáles de las cuatro fuentes se encontraron/omitieron.

---

### Tarea 8: Actualizaciones del instalador

**Archivos:**
- Modificar: `bin/components/dev-skills.sh:14`, `bin/components/ui-ux.sh:1-46`, `uninstall.sh:77-81`, `install.sh:212-228`

- [ ] **Paso 1: dev-skills.sh — nueva lista de skills propios**

Línea 14:

```bash
FIRST_PARTY_SKILLS=(architecture-principles conventional-commits)
```

También actualizar el comentario de cabecera (líneas 2–7) y el texto de la línea 17 de `ac_step` para nombrar la nueva lista.

- [ ] **Paso 2: ui-ux.sh — copiar el fork en vez de clonar el upstream**

Reemplazar `ac_uiux_install_skill()` (líneas 17–46) por completo con:

```bash
ac_uiux_install_skill() {
    local src="$AC_REPO_DIR/skills/ui-ux-pro-max"
    local dst="$CLAUDE_CONFIG_DIR/skills/ui-ux-pro-max"
    ac_info "Installing ui-ux-pro-max skill (first-party) into $dst"

    if [ ! -d "$src" ]; then
        ac_warn "Source skill missing: $src — skipping."
        return 0
    fi
    if [ "${DRY_RUN:-0}" = "1" ]; then
        ac_dim "\$ cp -R $src $dst"
        return 0
    fi
    mkdir -p "$CLAUDE_CONFIG_DIR/skills"
    rm -rf "$dst"
    cp -R "$src" "$dst"
}
```

Eliminar la línea 7 `UI_UX_SKILL_REPO=`, ahora sin uso, y actualizar el comentario de cabecera de la línea 3.

- [ ] **Paso 3: uninstall.sh — añadir architecture-principles a la limpieza**

El bucle de la línea 79 gana el nuevo skill (los nombres legados se mantienen para instalaciones antiguas):

```bash
for s in superpowers solid design-patterns conventional-commits architecture-patterns architecture-principles; do
```

- [ ] **Paso 4: install.sh — mensaje final**

En el heredoc de cierre, reemplazar la vieja lista de comandos de skill (línea 225) con:

```
       /architecture-principles /conventional-commits
```

y quitar cualquier línea `/repomap`, `/dcp-*` restante si las tareas 2–3 dejaron alguna.

- [ ] **Paso 5: Prueba de dry-run**

Ejecutar: `bash install.sh --dry-run --all`
Esperado: sale con 0; la salida muestra los componentes `rtk caveman figma ui-ux dev-skills`; sin mención de repo-map/dcp; el paso ui-ux imprime `cp -R .../skills/ui-ux-pro-max ...` (sin git clone).

Ejecutar: `bash uninstall.sh --dry-run`
Esperado: sale con 0; incluye las líneas de limpieza legada de dcp-lite/repo-map y la eliminación de `architecture-principles`.

- [ ] **Paso 6: Commit**

```bash
git add bin/components/dev-skills.sh bin/components/ui-ux.sh uninstall.sh install.sh
git commit -m "feat(installer): wire Skills 2.0 set, drop repo-map/dcp"
```

---

### Tarea 9: Documentación + verificación completa

**Archivos:**
- Modificar: `README.md`, `INSTALL.md`

- [ ] **Paso 1: Actualizar la documentación**

En ambos archivos: eliminar filas/secciones de repo-map y dcp-lite (grep: `grep -n "repo-map\|dcp" README.md INSTALL.md`); reemplazar las listas de skills por las tres nuevas (architecture-principles, conventional-commits, ui-ux-pro-max); documentar el formato de sidecar en el README bajo un nuevo encabezado "Skills 2.0 format" (copiar el bloque de formato de la sección "Skills 2.0 format" de la spec); anotar que ui-ux-pro-max ahora es propio (sin auto-actualización desde el upstream).

- [ ] **Paso 2: Batería de verificación completa**

```bash
node skills/validate-skills.mjs          # OK (3 skills)
bash install.sh --dry-run --all          # exit 0, correct component list
bash install.sh --all --config-dir /tmp/cm-test --no-npm   # real install to sandbox
ls /tmp/cm-test/skills                   # architecture-principles conventional-commits ui-ux-pro-max superpowers
cat /tmp/cm-test/skills/architecture-principles/skill.yaml  # present, correct
bash uninstall.sh --dry-run              # exit 0
rm -rf /tmp/cm-test
```

Esperado: todos los comandos salen con 0; el listado del sandbox muestra exactamente los cuatro directorios de arriba (más lo que escriban caveman/rtk).

- [ ] **Paso 3: Smoke test en Claude Code**

Reiniciar Claude Code, luego invocar `architecture-principles` y `ui-ux-pro-max` mediante la herramienta Skill en una sesión de prueba; confirmar que ambos cargan y mencionan sus sidecars.

- [ ] **Paso 4: Commit**

```bash
git add README.md INSTALL.md
git commit -m "docs: update for Skills 2.0 set"
```
